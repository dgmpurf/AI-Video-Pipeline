[project]
name = "ai_video_pipeline"
version = "0.1.0"
description = "Phase A skeleton for AI video production pipeline"
requires-python = ">=3.10"
dependencies = []

[project.urls]
Homepage = "https://example.com/ai_video_pipeline"

[tool.pytest.ini_options]
addopts = "-q"
