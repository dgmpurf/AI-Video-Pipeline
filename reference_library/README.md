# AI 视频模仿参考库运行目录

这里是未来真实本地参考库运行目录。

视频文件、派生缩略图、日志和 live sqlite 默认不进 Git。

所有新导入视频默认：

- active_input_allowed=false
- offline_grammar_only=true

active input 必须人工审核。

当前目录结构仅为 L1 local artifacts / directory setup，占位文件用于保留目录；本任务不导入媒体、不扫描视频、不生成缩略图、contact sheet 或 keyframe。
